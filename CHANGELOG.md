# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-17

首个版本。

### 新增

- 音频导入：MP3 / WAV / FLAC / OGG 等（经 miniaudio 解码，全部本地完成，无任何网络请求）。
- 两种卡点算法：
  - **Onset 瞬态起音检测**（默认）：相对谱通量 + 自适应中值阈值 + 局部极大值判定，等效 aubio 默认检测器思路；对持续长音、持续人声不误触发。
  - **音量峰值检测**：振幅包络局部极大值，并加"突出度"约束，避免恒定长音产生伪峰。
- 可调参数：灵敏度阈值（0–100）、最小卡点间隔（默认 0.15 秒）、全局时间偏移（毫秒，可正可负）。
- 波形预览：波形填充绘制 + 卡点竖线标记 + 秒刻度；播放试听并显示时间指针；点击波形跳转播放位置。
- 导出 TXT（每行一个秒数）与 CSV（时间(秒), 时间(毫秒)，带 BOM，Excel 打开不乱码）。
- 重置按钮：一键清空音频、波形与卡点标记。
- 命令行自检模式 `--selftest`（不开界面即可验证算法结果）、`--version`、`--help`。
- PyInstaller 打包脚本：单文件版 + 文件夹版；spec 中剔除 Tcl 的 tzdata 与 msgs 资源（省掉 700 多个文件），单文件版启动时间由 84 秒降到约 16 秒。

### 修复

- 中文路径 / 中文文件名下解码失败：miniaudio 按文件名打开时不支持非 ASCII 路径，改为先由 Python 读入内存再解码。
- 波形绘制坐标顺序错误导致显示成一团交叉线，改为标准闭合多边形（上沿正序 + 下沿逆序）。
- 参数面板过宽导致窗口变窄时"全局时间偏移"输入框被挤出可见区域，改为两行布局，并根据控件实际需求宽度自动设置最小窗口尺寸。
- 播放回调生成器缺少预热步骤导致播放报错。
- 下拉框宽度不足以完整显示算法名称。

### 已知限制

- aubio 无 Windows 预编译包，默认使用内置等效实现；若本机已装 aubio 会自动优先调用。
- iOS / Android 未适配（Tkinter 桌面程序）。
